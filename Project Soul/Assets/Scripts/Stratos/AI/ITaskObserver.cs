using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ITaskObserver : MonoBehaviour
{
    /*
     * RESERVED FOR FUTURE USE BY AI_MANAGER
     * 
     * This interface will have the goal to provide communication between AI_Manager and each entity that 
     * can be managed/ordered to perform specific tasks.
     * RELATED PAPER:
     * https://www.diva-portal.org/smash/get/diva2:831699/FULLTEXT01.pdf
     * 
     */
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
